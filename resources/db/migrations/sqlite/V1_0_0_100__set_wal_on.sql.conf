#
# In order to enable WAL, the PRAGMA directive that sets it on, must
# be run outside of a transaction, otherwise an error will occur, i.e.,
#
# [SQLITE_ERROR] SQL error or missing database (cannot change into wal mode from within a transaction)
#
executeInTransaction=false
